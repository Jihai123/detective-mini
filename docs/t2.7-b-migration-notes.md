# T2.7-B 迁移记录

## 范围

- `src/cases/case-001/data.ts` 迁移到 `src/cases/case-001/data.json`
- `src/stage1/caseLoader.ts`：填充 `JSON_CASE_REGISTRY`，module-load 时一次性 `validateCaseConfig`
- `src/cases/case-001/index.ts`：改为从 `data.json` 静态 import，`data.ts` 删除

## 决策（已落地）

- **D1** 机械 1:1 翻译，零业务修改（`JSON.stringify` 严格相等验证通过）
- **D2** `ConfrontationConfig` 双格式（`rounds` + `suspects`）双保留
- **D3** `TestimonySentenceResponses` 等零填充字段在 JSON 中省略
- **D4** 路径字段 16 处保留完整 `/assets/cases/case-001/{cat}/{filename}` 字符串
- **D5** 决策 A 静态 import 落地，JSON 内联 JS bundle（dist/ 中无独立 JSON emit）
- **D6** `caseLoader` API 同步签名不变
- **D7** `data.ts` 完全删除，`index.ts` 改为直接 import `data.json`
- **D8** JSON 路径在 `src/`（静态 import 必须 `src/`，不是 `public/`）

## 数据规模

- `data.json` 原始 22,183 bytes / gzip 5,178 bytes
- Bundle 影响：JS gzip 22.30 → 22.32 kB（+0.02 kB）/ CSS 不变（6.61 kB）

## 行为不变验证

真人浏览器实测 6 路径全过（用户执行，未用 console 日志或 mental simulation 替代）：

- happy path（图片加载、对质流程、result 屏）
- misread 扣容错
- irrelevant 不扣容错
- 全 misread → failure 屏
- 硬刷恢复（经 case selector 中转，容错数正确）
- 跨 case 切换进度保留
- F12 Network 全程无 4xx/5xx
- F12 Console 全程无 error

result 屏文本逐字与 T2.6-A 锁定值一致：

- 标题：案件归档
- body：你已锁定真相核心：周岚在会前拆封并转移结论页。

（success / failure 文本目前一致，是 T3 任务范围，与 T2.7-B 无关）

## 延期任务

T2.7 决策 C（JSON 写文件名 + helper 拼接）在 case-001 的 data 字段层面未完整落地。具体：

- 16 处素材路径字段在 JSON 中仍是完整路径字符串
- 12 处消费方仍直接读取这些字段（2 处 `ClueConfig.image` / 4 处 `SceneConfig.background` / 6 处 `CharacterConfig.* portrait / avatar`）
- `caseLoader.ts` 中 `getSceneBackground(_sceneId, fallback)` 的 `_sceneId` 参数被忽略，直接 `return fallback`

后续延期任务范围（命名待用户裁定）：

- 12 处消费方改用 `getCaseAssetPath(this.state.caseId, ...)` 拼路径
- 修复 `getSceneBackground` 假参数（让 `sceneId` 真起作用，或删除该 wrapper）
- JSON 中 16 个路径字段值改为只写文件名
- `docs/case-asset-conventions.md` 完整命名约定文档（在路径整理收尾后写，避免约定与代码不一致）

## 新增 KNOWN_ISSUES 项

"case-001 data 字段层路径未文件名化"挂在 `PROJECT_STATE.md` KNOWN_ISSUES P2 技术债清单（用户合并 main 时更新）。

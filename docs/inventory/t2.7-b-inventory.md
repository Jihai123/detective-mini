# T2.7-B 盘点报告：case-001 数据迁移 JSON 阶段一盘点

**盘点日期**：2026-04-30  
**基线 commit**：`2540933` (main)  
**分支**：`feat/t2.7-b-case-001-json-migration`  
**Bundle 基线**：JS gzip 22.30 KB / CSS gzip 6.61 KB

---

## 维度 1：case-001 现有 TS 数据全景

### 1.1 文件总行数

| 文件 | 行数 |
|---|---|
| `src/cases/case-001/data.ts` | 184 |
| `src/cases/case-001/index.ts` | 14 |

### 1.2 index.ts 全文

```ts
import type { CaseDefinition } from '../types';
import { case001Config } from './data';

export const case001: CaseDefinition = {
  meta: {
    id: 'case-001',
    title: '08:17 的空档',
    difficulty: 'tutorial',
    tutorialMode: true,
    order: 1,
    unlocked: true,
  },
  config: case001Config,
};
```

### 1.3 顶层导出节点清单

**data.ts** 导出 1 个顶层节点：

| 名称 | 类型 | 行号区间 |
|---|---|---|
| `case001Config` | `StageCaseConfig` | 3–184 |

**data.ts 中 `case001Config` 的直接字段（第二层节点）**：

| 字段 | 类型 | 行号 |
|---|---|---|
| `id` | `string` | 4 |
| `title` | `string` | 5 |
| `summary` | `string` | 6 |
| `timeRange` | `string` | 7 |
| `location` | `string` | 8 |
| `introLines` | `string[]` | 9–13 |
| `initialObjective` | `string` | 14 |
| `clues` | `ClueConfig[]` | 15–20 |
| `testimonies` | `TestimonyConfig[]` | 21–26 |
| `characters` | `CharacterConfig[]` | 27–30 |
| `dialogueNodes` | `DialogueNode[]` | 31–37 |
| `scenes` | `SceneConfig[]` | 38–49 |
| `confrontation` | `ConfrontationConfig` | 50–140 |
| `timelineSlots` | `TimelineSlot[]` | 141–146 |
| `submission` | `SubmissionConfig` | 147–158 |
| `truthReplay` | `TruthReplaySegment[]` | 159–164 |
| `endings` | `Record<string, EndingTextBlock>` | 169–178 |
| `endingMatrix` | `EndingMatrix` | 180–183 |

**index.ts** 导出 1 个顶层节点：

| 名称 | 类型 | 行号区间 |
|---|---|---|
| `case001` | `CaseDefinition` | 4–14 |

### 1.4 各顶层字段递归层级深度

从 `case001Config` 计为第 1 层：

| 路径（最深路径示例） | 深度 |
|---|---|
| `confrontation.suspects[].rounds[].sentences[].counterEvidenceId` | 6 |
| `clues[].interpretations.canonical.attacksTestimonyIds[]` | 5 |
| `scenes[].hotspots[].onInteract[].clueId` | 5 |
| `dialogueNodes[].options[].unlockCondition` | 4 |
| `characters[].emotionPortraits.neutral` | 4 |
| `submission.correct.suspect` | 3 |
| `endings.success.title` | 3 |
| `endingMatrix.rules[].when.submissionCorrect` | 4 |

最大嵌套深度：**6 层**（`confrontation.suspects[].rounds[].sentences[].counterEvidenceId`）

---

## 维度 2：JSON 序列化可行性穷举

### 2.1 ConfrontationConfig：rounds vs suspects 双格式

两种格式**同时存在**于 case-001 数据中：

| 格式 | 字段路径 | 行号区间 |
|---|---|---|
| 旧格式（顶层 rounds） | `confrontation.rounds` | 53–90 |
| 新格式（suspects 数组） | `confrontation.suspects` | 94–139 |
| `confrontation.target` | `'zhoulan'` | 51 |
| `confrontation.maxMistakesPerRound` | `2` | 52 |
| `confrontation.suspects[0].suspectId` | `'zhoulan'` | 96 |
| `confrontation.suspects[0].maxMistakesPerRound` | `2` | 97 |

两个格式的 rounds 内容完全相同（3 轮，每轮 2 条 sentences）。

### 2.2 ClueRole 枚举 JSON 兼容性

类型定义（`src/stage1/types.ts`）：`'confrontation' | 'emotional' | 'context'`

case-001 中实际值：全部 4 条 clue 的 `role` 字段均为字符串字面量 `'confrontation'`（行 16、17、18、19）。

`JSON.parse(JSON.stringify(...))` 后：值仍为 `'confrontation'`（string 类型），与 `ClueRole` 联合类型成员完全一致。JSON 不携带类型信息，反序列化后为宽 `string`，TS 类型收窄须由调用侧 `validateCaseConfig` 保证。

### 2.3 EndingMatrixRule.when 条件

类型定义（`src/stage1/types.ts:207–213`）：
```ts
when: {
  minScore?: number;
  submissionCorrect?: boolean;
}
```

case-001 中实际使用（`data.ts:181`）：

| key | 值类型 | 实际值 |
|---|---|---|
| `submissionCorrect` | `boolean` | `true` |

`minScore` 在 case-001 中未使用。

### 2.4 TestimonySentenceResponses 4 层 fallback 填充情况

类型定义（`src/stage1/types.ts`）：`{ partial?: string; misread?: string; irrelevant?: string }`

fallback 链（从数据到运行时全局默认）：

| 层级 | 字段 | case-001 data.ts 中填充情况 |
|---|---|---|
| 第 1 层 | `responses.partial` | 0 条 sentence 填充（全部缺省） |
| 第 2 层 | `responses.misread` | 0 条 sentence 填充（全部缺省） |
| 第 3 层 | `responses.irrelevant` | 0 条 sentence 填充（全部缺省） |
| 第 4 层 | 运行时全局默认 | 在数据中无对应字段 |

case-001 全部 12 条 `TestimonySentence`（6 条 × 旧格式 + 6 条 × suspects 格式）均无 `responses` 字段。

### 2.5 discoveryLayers / unlockRequirement 使用情况

| 字段 | case-001 是否使用 | 实证 |
|---|---|---|
| `discoveryLayers` | **是** | 全部 4 条 clue 均含 `discoveryLayers`，每条含 1 个 layer（行 16、17、18、19） |
| `unlockRequirement` | **否** | data.ts 全文无 `unlockRequirement` 字段 |

`discoveryLayers[0]` 结构：`{ layerId: 'layer-1', description: string }`，无 `unlockAction` 字段。

### 2.6 非 JSON 兼容类型（function / Map / Set / Date / undefined / Symbol / class 实例）

**零项**。data.ts 全文（184 行）不含：
- `new`关键字
- 箭头函数 `=>`
- `function` 声明
- `Map`、`Set`、`Date`、`Symbol`、`undefined`

grep 验证：`grep -n "new \|() =>\|=> {" src/cases/case-001/data.ts` 无输出。

### 2.7 TS-only 类型断言

**零项**。grep 验证：`grep -n "as const\|satisfies\|as [A-Z]" src/cases/case-001/data.ts` 无输出。

唯一的 TS 成分是第 1 行 `import type` 和第 3 行的类型标注 `: StageCaseConfig`，两者在 JSON 序列化中不涉及。

---

## 维度 3：素材路径在 case-001 数据中的当前形态

字段名为 `image`（非 `imagePath`），类型定义见 `src/stage1/types.ts` `ClueConfig.image?: string`。

### 3.1 clues[].image

| JSONPath | 当前字段值 |
|---|---|
| `clues[0].image` | `/assets/cases/case-001/clues/clue-envelope-opened.jpg` |
| `clues[1].image` | `/assets/cases/case-001/clues/clue-doorlog-0728.jpg` |
| `clues[2].image` | `/assets/cases/case-001/clues/clue-camera-gap-0731.jpg` |
| `clues[3].image` | `/assets/cases/case-001/clues/clue-shred-label.jpg` |

### 3.2 characters[].avatar / portrait / emotionPortraits

| JSONPath | 当前字段值 |
|---|---|
| `characters[0].avatar` | `/assets/cases/case-001/characters/zhoulan-avatar.png` |
| `characters[0].portrait` | `/assets/cases/case-001/characters/zhoulan-neutral.png` |
| `characters[0].emotionPortraits.neutral` | `/assets/cases/case-001/characters/zhoulan-neutral.png` |
| `characters[0].emotionPortraits.defensive` | `/assets/cases/case-001/characters/zhoulan-defensive.png` |
| `characters[0].emotionPortraits.collapse` | `/assets/cases/case-001/characters/zhoulan-collapse.png` |
| `characters[1].avatar` | `/assets/cases/case-001/characters/chenxu-avatar.png` |
| `characters[1].portrait` | `/assets/cases/case-001/characters/chenxu-neutral.png` |
| `characters[1].emotionPortraits.neutral` | `/assets/cases/case-001/characters/chenxu-neutral.png` |
| `characters[1].emotionPortraits.serious` | `/assets/cases/case-001/characters/chenxu-serious.png` |

### 3.3 scenes[].background

| JSONPath | 当前字段值 |
|---|---|
| `scenes[0].background` | `/assets/cases/case-001/scenes/review_room.jpg` |
| `scenes[1].background` | `/assets/cases/case-001/scenes/hallway_monitor.jpg` |
| `scenes[2].background` | `/assets/cases/case-001/scenes/pantry_bin.jpg` |

### 3.4 路径形态总结

- **T2.7-A 后是否已经只写文件名**：**否**。data.ts 中全部 16 个路径字段均保留完整的 `/assets/cases/case-001/{category}/{filename}` 绝对路径格式。
- 含 `/assets/` 前缀：全部 16 个路径字段（4 × clue image + 9 × character portrait/avatar + 3 × scene background）。
- 含 `case-001` 前缀：全部 16 个路径字段。

---

## 维度 4：数据加载链路

### 4.1 StageOneApp 构造时获取 StageCaseConfig 的调用链

`src/stage1/app.ts:77–81`：

```ts
constructor({ root, caseId, onExit }: { root: HTMLElement; caseId: string; onExit?: () => void }) {
  this.root = root;
  this.onExit = onExit;
  const existingSave = loadStageSave(caseId);
  const caseConfig = loadCaseConfig(existingSave?.caseId ?? caseId);
```

调用链：`StageOneApp constructor` → `loadCaseConfig(caseId)` → `JSON_CASE_REGISTRY[caseId]`（未命中） → `getCaseById(caseId).config!` → `validateCaseConfig(config)` → 返回 `StageCaseConfig`

### 4.2 loadCaseConfig 当前实现

`src/stage1/caseLoader.ts:58–67`：

```ts
export function loadCaseConfig(caseId: string): StageCaseConfig {
  const jsonConfig = JSON_CASE_REGISTRY[caseId];
  if (jsonConfig !== undefined) {
    validateCaseConfig(jsonConfig);
    return jsonConfig;
  }
  const config = getCaseById(caseId).config!;
  validateCaseConfig(config);
  return config;
}
```

### 4.3 JSON_CASE_REGISTRY 当前状态

`src/stage1/caseLoader.ts:5`：

```ts
const JSON_CASE_REGISTRY: Record<string, StageCaseConfig> = {};
```

**当前为空对象**：`{}` — 无任何 key。

### 4.4 validateCaseConfig 签名与校验维度

签名（`src/stage1/caseLoader.ts:31`）：

```ts
export function validateCaseConfig(input: unknown): asserts input is StageCaseConfig
```

校验维度清单（按执行顺序）：

| # | 校验内容 | 代码行 |
|---|---|---|
| 1 | input 为非 null 对象 | 32 |
| 2 | id / title / summary / timeRange / location 非空 | 34 |
| 3 | scenes / clues / characters / dialogueNodes 为数组 | 35 |
| 4 | confrontation 字段存在 | 36 |
| 5 | confrontation 含 rounds（旧格式）或非空 suspects 数组（新格式），至少一项 | 38–40 |
| 6 | timelineSlots 为数组 / submission 存在 / truthReplay 为数组 | 41 |
| 7 | 每个 scene：id / background / label 存在 | 44 |
| 8 | 每个 scene：unlockCondition 格式合法 | 45 |
| 9 | 每个 scene 下每个 hotspot 校验（validateHotspot） | 46 |
| 10 | hotspot：id / label / sceneId 存在 | 18 |
| 11 | hotspot：positionMode 为 `'percent'` | 19 |
| 12 | hotspot：position.x / position.y 为 number | 20 |
| 13 | hotspot：unlockCondition 格式合法 | 21 |
| 14 | hotspot：onInteract 为非空数组 | 22 |
| 15 | 每个 dialogueNode 校验（validateDialogueNode） | 49 |
| 16 | dialogueNode：id / characterId / lines（数组）/ options（数组）存在 | 26 |
| 17 | dialogueNode：condition / unlockCondition 格式合法 | 27–28 |
| 18 | 每个 confrontation round：id 存在 / sentences 为非空数组 | 54 |

---

## 维度 5：Vite JSON import 行为实证

### 5.1 配置文件扫描结果

| 文件 | 是否存在 | json / resolveJsonModule 相关行 |
|---|---|---|
| `vite.config.ts` | **否**（项目根目录无此文件） | — |
| `vite.config.js` | **否** | — |
| `tsconfig.json` | 是 | 无 `resolveJsonModule`，无 json 相关配置行 |

`tsconfig.json` 全部 `compilerOptions` 键：`target` / `module` / `lib` / `types` / `skipLibCheck` / `moduleResolution` / `allowImportingTsExtensions` / `verbatimModuleSyntax` / `moduleDetection` / `noEmit` / `noUnusedLocals` / `noUnusedParameters` / `erasableSyntaxOnly` / `noFallthroughCasesInSwitch`。

### 5.2 静态 import JSON 的产物行为

项目使用 Vite 默认配置（无 `vite.config.ts`）。Vite 默认对 JSON 文件执行内联转换：静态 `import data from './case-001.json'` 产物进入**同一 bundle chunk**（不单独 emit 为独立文件）。`assert { type: 'json' }` 语法在 Vite 中不影响此行为；JSON 内容被序列化为 JS 对象字面量并内联到 importer 所在 chunk。

---

## 维度 6：case-001 数据 gzip 体积估算

使用 Node.js `zlib.gzipSync` 对 `JSON.stringify(case001Config)` 产物实测（`/tmp/case001_eval.js` 提取自 data.ts）：

| 指标 | 值 |
|---|---|
| JSON.stringify 原始字节数（无缩进） | **15,532 bytes** |
| JSON.stringify 原始字节数（2-space 缩进） | **22,182 bytes** |
| gzip 后字节数（基于 2-space 缩进 JSON） | **5,177 bytes** |

此数值作为后续 bundle 验收基准：JSON 文件（含格式化）内联后 gzip 增量约 **5.1 KB**。

---

## 维度 7：删除 data.ts 后的 import 引用追踪

### 7.1 直接 import src/cases/case-001/data 的文件

```
src/cases/case-001/index.ts:2:  import { case001Config } from './data';
```

全仓库仅 1 处直接引用 `./data`。

### 7.2 import src/cases/case-001（目录形式）的文件

```
src/cases/index.ts:2:  import { case001 } from './case-001';
```

全仓库仅 1 处以目录形式引用（解析到 index.ts）。

### 7.3 src/cases/case-001/index.ts 当前导出

（见维度 1.2 全文）

导出：`case001`（类型 `CaseDefinition`），内含 `meta` + `config`（引用自 `case001Config`）。

### 7.4 删除 data.ts 后需要改动 import 的文件（逐项）

| 文件 | 行号 | 当前 import 语句 | 原因 |
|---|---|---|---|
| `src/cases/case-001/index.ts` | 2 | `import { case001Config } from './data';` | `./data` 文件消失，需改为从 JSON 或其他来源取 config |

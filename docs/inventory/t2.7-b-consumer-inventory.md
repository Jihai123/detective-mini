# T2.7-B 微盘点报告：case-001 路径字段消费方追踪

**盘点日期**：2026-04-30  
**前置 commit**：`90e232c`（阶段一盘点）  
**分支**：`feat/t2.7-b-case-001-json-migration`

---

## 预备说明：字段名称实证

盘点任务使用了 `imagePath` / `portraits` 两个字段名。实际字段名与类型定义（`src/stage1/types.ts`）不符，实证如下：

| 任务假设字段名 | 实际字段名 | 类型定义位置 |
|---|---|---|
| `ClueConfig.imagePath` | `ClueConfig.image` | `types.ts: image?: string` |
| `CharacterConfig.portraits` | `CharacterConfig.portrait`（单数）+ `CharacterConfig.emotionPortraits` | `types.ts: portrait: string` / `emotionPortraits?: Partial<Record<Emotion, string>>` |

以下各节同时报告假设名称的 grep 结果（零命中）与实际字段名的 grep 结果。

---

## (1) ClueConfig.image 消费方追踪

### grep `.imagePath` / `['imagePath']` / `{ imagePath` 结果

**零命中**。全仓库 src/ 下（排除 types.ts、case-001/data.ts）无任何 `.imagePath` 访问。

### grep `.image`（实际字段名）结果

| 文件:行号 | 完整代码行 | 用途 |
|---|---|---|
| `src/stage1/app.ts:215` | `...caseConfig.clues.map((c) => c.image).filter((v): v is string => Boolean(v)),` | 预加载全部 clue 图片，读取 `c.image` 直接用于 `preloadImage` |
| `src/stage1/app.ts:1047` | `${clue?.image ? \`<img class="inspect-image" src="${clue.image}" alt="${clue.title}" onerror="this.src='${FALLBACK_PATHS.scene}'" />\` : ''}` | 渲染 inspect overlay，读取 `clue?.image`（条件判断）和 `clue.image`（img src 属性）两处 |

**ClueConfig.image 消费方共 2 处（app.ts:215、app.ts:1047）**，均直接将字段值（完整路径）用于 `<img src>` 或 `preloadImage`，无中间转换。

---

## (2) SceneConfig.background 消费方追踪

### grep `.background` / `['background']` / `{ background`（排除 CSS，仅 .ts/.tsx）结果

| 文件:行号 | 完整代码行 | 用途 |
|---|---|---|
| `src/stage1/caseLoader.ts:44` | `if (!scene.id \|\| !scene.background \|\| !scene.label) throw new Error(...)` | 校验字段存在性（truthiness 检查），不读取路径值 |
| `src/stage1/app.ts:196` | `this.preloadImage(this.getSceneBackground(scene.id, scene.background)),` | 预加载当前场景背景，将 `scene.background` 传入 `getSceneBackground` |
| `src/stage1/app.ts:213` | `...caseConfig.scenes.map((s) => this.getSceneBackground(s.id, s.background)),` | 预加载全部场景背景，将每个 `s.background` 传入 `getSceneBackground` |
| `src/stage1/app.ts:1460` | `${this.getScreenBody(caseConfig.scenes.find((s) => s.id === this.state.currentSceneId)?.background ?? caseConfig.scenes[0].background)}` | 渲染主体，将 `?.background` 值传入 `getScreenBody(background)` → `getSceneBackground` |

**`getSceneBackground` 方法实现**（`app.ts:1013–1015`）：

```ts
private getSceneBackground(_sceneId: string, fallback: string): string {
  return fallback;
}
```

`_sceneId` 参数未使用，方法直接返回 `fallback`（即 `scene.background` 的原始值）。

**SceneConfig.background 消费方共 4 处**：
- `caseLoader.ts:44`：存在性校验（不消费路径字符串值）
- `app.ts:196`、`app.ts:213`、`app.ts:1460`：3 处将完整路径值直接传递至 `preloadImage` 或 HTML `background-image`，无中间转换。

---

## (3) CharacterConfig.portrait / avatar / emotionPortraits 消费方追踪

### grep `.portraits` / `['portraits']` / `{ portraits` 结果

**零命中**。全仓库 src/ 下（排除 types.ts、case-001/data.ts）无任何 `.portraits` 访问。

### grep 实际字段名（`.avatar` / `.portrait` / `.emotionPortraits`）结果

| 文件:行号 | 字段 | 完整代码行 | 用途 |
|---|---|---|---|
| `src/stage1/app.ts:193` | `.avatar` | `.map((id) => this.getCharacterVisual(caseConfig.characters.find((c) => c.id === id))?.avatar)` | 预加载当前场景人物 avatar；经由 `getCharacterVisual` 返回 `character.avatar` |
| `src/stage1/app.ts:214` | `.portrait` | `...caseConfig.characters.map((c) => this.getCharacterVisual(c)?.portrait ?? c.portrait),` | 预加载全部人物 portrait；经由 `getCharacterVisual` 返回 `character.portrait`，fallback 为 `c.portrait` |
| `src/stage1/app.ts:1019` | `.avatar` `.portrait` | `return { avatar: character.avatar, portrait: character.portrait };` | `getCharacterVisual` 方法体，直接读取两个字段组成返回对象 |
| `src/stage1/app.ts:1031` | `.avatar`（×2） | `<img src="${visual?.avatar ?? character.avatar}" ... onerror="this.src='${character.avatar}'"` | 渲染 character card，`visual?.avatar` 主路径，`character.avatar` 作 fallback 及 onerror |
| `src/stage1/app.ts:1118` | `.portrait`（×2） `.avatar`（×2） | `<img class="portrait" src="${visual?.portrait ?? character.portrait}" ... onerror="this.src='${character.portrait}'" /><img class="avatar-badge" src="${visual?.avatar ?? character.avatar}" ... onerror="this.src='${character.avatar}'"` | 渲染 dialogue overlay，portrait 和 avatar 各有主路径 + onerror 两处读取 |
| `src/stage1/app.ts:1132` | `.emotionPortraits` `.portrait` | `const portraitSrc = currentChar?.emotionPortraits?.[emotion] ?? currentChar?.portrait ?? FALLBACK_PATHS.portrait;` | 渲染 confrontation body，按情绪索引 `emotionPortraits`，fallback 到 `portrait`，再 fallback 到全局常量 |

**CharacterConfig 路径字段消费方共 6 处**（`app.ts:193、214、1019、1031、1118、1132`），均直接将字段值（完整路径）用于 `<img src>`、onerror、或 `preloadImage`，无中间转换。

---

## (4) 其他路径字段穷举

### (4a) 额外字段名扫描

| 字段名 | grep 结果 |
|---|---|
| `.thumbnailPath` | 零命中 |
| `.detailImagePath` | 零命中 |
| `.avatarPath` | 零命中 |

以上字段在 `ClueConfig` / `CharacterConfig` 类型定义及全仓库 src/ 中均不存在。

### (4b) 全仓库含 `/assets/cases/` 字符串（排除 data.ts）

| 文件:行号 | 完整代码行 | 来源类型 |
|---|---|---|
| `src/case-selector/case-selector.ts:57` | `` `<div class="directory-card-cover" style="background-image:url('/assets/cases/${meta.id}/scenes/archive_cover.jpg')"></div>` `` | 硬编码模板字符串（动态 caseId），不读取 data.ts 字段 |
| `src/cases/case-paths.ts:6` | `return \`/assets/cases/${caseId}/${category}/${filename}\`;` | `getCaseAssetPath` 函数体（路径生成函数，非字段读取） |
| `src/cases/case-paths.ts:10` | `scene: '/assets/cases/case-001/scenes/scene-fallback.jpg',` | `FALLBACK_PATHS.scene` 常量定义 |
| `src/cases/case-paths.ts:11` | `portrait: '/assets/cases/case-001/characters/portrait-fallback.png',` | `FALLBACK_PATHS.portrait` 常量定义 |

以上 4 处均不读取 `case-001/data.ts` 中的路径字段，为独立的路径字符串定义或生成逻辑。

---

## 小结

- **ClueConfig.image 在 2 处被消费方读取**：`src/stage1/app.ts:215`、`src/stage1/app.ts:1047`
  - 注：任务假设字段名 `imagePath` — 实际字段名为 `image`，`imagePath` 零命中
- **SceneConfig.background 在 4 处被消费方读取**：`src/stage1/caseLoader.ts:44`、`src/stage1/app.ts:196`、`src/stage1/app.ts:213`、`src/stage1/app.ts:1460`
  - 其中 `caseLoader.ts:44` 为存在性校验（不使用路径字符串值），其余 3 处将路径值直接用于渲染/预加载
- **CharacterConfig.portrait/avatar/emotionPortraits 在 6 处被消费方读取**：`src/stage1/app.ts:193、214、1019、1031、1118、1132`
  - 注：任务假设字段名 `portraits` — 实际字段名为 `portrait`（singular）和 `emotionPortraits`，`portraits` 零命中
- **其他路径字段读取：0 处，清单如下**：`.thumbnailPath`、`.detailImagePath`、`.avatarPath` 均不存在于任何文件
- **消费方读取总数：12 处**（2 + 4 + 6，含 caseLoader.ts 的存在性校验 1 处）

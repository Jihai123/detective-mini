# T2.8.1 — case-002 数据接入修复(render 层数据驱动改造)

## 0. 上下文

T2.8 完成后用户实测 case-002,8 个 bug 全部暴露。诊断 commit 9fe259b
报告根因:**T2.6-B / T2.7-B 时代 render 层为单 case 写的硬编码,
case-002 上线时全部错位**。

这不是 T2.8 数据填错的问题,是更早期的架构债务被 case-002 揭盖。

T2.8.1 用路径 A(全数据驱动改造)修复:7 个 render 函数全部改成读
caseConfig 字段,case-001 数据补字段(从硬编码反推),case-002 数据
补缺字段。

## 1. 分支

- 分支名:feat/t2.8.1-case-002-data-flow-fix
- 基线:最新 main(T2.8 已合)
- Bundle 基线:JS gzip 35.16 / CSS gzip 6.61
- 合并方式:GitHub PR merge

## 2. 范围

### 改造目标
- 7 个 render 函数从硬编码改为读 caseConfig
- types.ts 扩展 CaseMeta + StageCaseConfig 加新字段
- case-001 数据补字段(reverse-engineer 现有硬编码文本)
- case-002 数据补缺字段(从三份剧本文档第一段抄录占位)
- 修复 selector renderCard / archive 第二格 / hotspot state 三处
  case-aware 判断

### 不做的事
- 不改 confrontation / deduction / result 屏(这三屏 T2.6 / T2.8 时已数据驱动)
- 不改 SAVE_VERSION
- 不改 schema 现有字段(只加新字段)
- 不重做 UI 视觉(那是 T2.10 的事)
- 不接 SFX(留 T2.9)
- 不写新文案(case-002 字段先用剧本第一段占位,用户后续微调)

## 3. 8 个 bug 修复对应表

| Bug | 根因 | 修复点 |
|---|---|---|
| 1 | CaseMeta 无 hero 字段;renderCard 只在 order===1 渲染封面 | CaseMeta 加 hero 字段;renderCard 改为字段驱动 |
| 2 | renderArchiveBody 第二格硬编码 DOM | 第二格改为遍历 CASE_REGISTRY 渲染 |
| 3 | renderArchiveBody / renderIntroBody 全文硬编码 case-001 | 全部改读 caseConfig.briefing 字段 |
| 4 | render 层忽略 this.state.caseId | 同 Bug 3 修复 |
| 5 | renderIntroBody 不读 introLines / initialObjective / characters | 同 Bug 3 修复 |
| 6 | getNextActions 硬编码 case-001 人物名 + evaluateFirstContradiction 硬编码 case-001 clue ID | 改为读 caseConfig.investigationFlow 数据驱动 |
| 7 | hotspot onInteract 缺 openOverlay;getHotspotState 硬编码 case-001 hotspot ID | hotspot 数据补 onInteract.openOverlay;getHotspotState 改为通用查找 |
| 8 | getNextActions 硬编码 case-001 文本 | 同 Bug 6 修复 |

## 4. types.ts 扩展(新增字段)

```typescript
// CaseMeta 扩展(选 selector 卡片视觉)
interface CaseMeta {
  // ... 现有字段
  hero?: {
    image: string;       // 卡片封面图,文件名(经 getCaseAssetPath 拼接)
    category: 'scenes' | 'characters' | 'clues';  // helper 拼路径用
    altText?: string;
  };
  tagline?: string;       // 卡片副文本(case-001 用"教学档案",case-002 用"标准难度")
}

// StageCaseConfig 扩展(briefing / investigationFlow)
interface StageCaseConfig {
  // ... 现有字段
  briefing?: {
    // archive 屏 + investigation briefing 屏共用
    title: string;                    // 案件标题(可与 meta.title 不同)
    location: string;                 // "北港生物研发中心 6 层"
    timeRange: string;                // "07:20 - 08:22"
    summary: string;                  // 案件摘要(一句话)
    incomingReport: string[];         // 接案简报(数组,多段)
    suspects: Array<{                 // 涉案人物概览
      name: string;
      role: string;                   // "行政助理,最后接触资料者"
    }>;
    initialObjective: string;         // "先确认谁在会前接触过结论页"
    riskLevel?: 'LOW' | 'MEDIUM' | 'HIGH';   // 风险等级 (case-002 显示)
    difficulty?: string;              // "TUTORIAL" | "NORMAL" | "HARD"(显示用)
    investigationTags?: string[];     // ["企业调查", "资料失窃"] 等小标签
    briefingHeroImage?: string;       // briefing 屏 hero 图(scene 类别文件名)
  };

  investigationFlow?: {
    // 引导玩家"下一步做什么"的数据驱动配置
    initialNextAction: string;        // 起始引导文本
    contradictionTriggers?: Array<{
      // 触发"第一矛盾"的条件
      requiredClueIds: string[];      // 玩家必须收集这些 clue
      thenSetFlag: string;            // 设置 flag(例 "first-contradiction-found")
    }>;
    flagBasedNextActions?: Array<{
      // 根据 flag 状态显示不同 nextAction
      whenFlag: string;
      thenShow: string;
    }>;
    suspectQuestionPrompts?: Array<{
      // "可追问人物"列表,case-aware
      suspectId: string;              // 对应 confrontation suspect id
      promptText: string;             // 列表显示的提示文本
    }>;
  };
}
```

case-001 / case-002 都必须填上述字段(下面 §5 §6 列填值方案)。

## 5. case-001 数据补字段方案(reverse-engineer)

### 5.1 hero(从硬编码反推)
现有 selector 卡片显示的"机密文件"hero 图,从 `public/assets/cases/case-001/scenes/`
找出当前用的那张文件名。Code 实施时通过 grep / view 找硬编码 hero 路径,
反推填入 case-001 data.json:

```json
"meta": {
  ...
  "hero": {
    "image": "<现有硬编码文件名>",
    "category": "scenes",
    "altText": "案件 001 封面"
  },
  "tagline": "教学档案"
}
```

如 grep 找不到现有 hero 硬编码,Code 停下汇报,等用户指定 hero 文件名。

### 5.2 briefing(逐字抄录现有 archive / intro 屏文本)

按用户图 4(case-001 进入调查 briefing 屏)文本反推:

```json
"briefing": {
  "title": "08:17 的空档",
  "location": "北港生物研发中心 6 层",
  "timeRange": "07:20 - 08:22",
  "summary": "评审会开始前,唯一纸质结论页失踪。",
  "incomingReport": [
    "07:20 资料送达,08:00 前结论页失踪。",
    "外部评委已在路上,你只有一轮窗口锁定接触链。"
  ],
  "suspects": [
    { "name": "周岚", "role": "行政助理,最后接触资料者" },
    { "name": "陈序", "role": "产品经理,提供侧面信息" }
  ],
  "initialObjective": "先确认谁在会前接触过结论页。",
  "riskLevel": "HIGH",
  "difficulty": "TUTORIAL",
  "investigationTags": ["企业调查", "资料失窃"],
  "briefingHeroImage": "<现有 case-001 briefing 屏背景图文件名>"
}
```

文本必须与现有硬编码**逐字一致**,这是 case-001 行为不变的关键。
Code 实施时通过 grep / view 抓取硬编码原文,逐字填入 JSON。

### 5.3 investigationFlow(反推现有 nextActions / 矛盾触发逻辑)

case-001 现在 evaluateFirstContradiction 检查的 clue ID 是什么?
Code 必须 grep 找出来填进 contradictionTriggers。

```json
"investigationFlow": {
  "initialNextAction": "进入会议室档案区,查找接触链证据。",
  "contradictionTriggers": [
    {
      "requiredClueIds": ["<现有硬编码 clue ID 列表>"],
      "thenSetFlag": "first-contradiction-found"
    }
  ],
  "flagBasedNextActions": [
    {
      "whenFlag": "first-contradiction-found",
      "thenShow": "追问周岚,确认\"封存后未触碰\"是否成立"
    }
  ],
  "suspectQuestionPrompts": [
    { "suspectId": "zhoulan", "promptText": "周岚:行政助理,可追问" },
    { "suspectId": "chenxu", "promptText": "陈序:产品经理,可追问" }
  ]
}
```

**关键**:`initialNextAction` 字段 case-001 现在硬编码什么文本?
Code 必须 grep 抓取填入。所有现有 case-001 引导文本必须 1:1 反推。

## 6. case-002 数据补字段方案(用剧本占位)

### 6.1 hero
case-002 selector 卡片应该用哪张图?候选:

- **scenes/meeting-room.jpg**(会议室外景,有死者+保温杯线索)
- **scenes/office-area.jpg**(办公区,常规感)

**默认选 meeting-room**(命案现场最有钩子)。Code 实施时若 case-002 scenes
目录没有 meeting-room.jpg,从实际文件名中选一张氛围最强的填入,commit
message 中说明选了哪张。

```json
"hero": {
  "image": "meeting-room.jpg",  // 或实际文件名
  "category": "scenes",
  "altText": "案件 002 封面"
},
"tagline": "标准难度"
```

### 6.2 briefing(从 testimony.md 第二部分场景描述抄录占位)

```json
"briefing": {
  "title": "08:17 的通话",
  "location": "北港生物研发中心 6 层",
  "timeRange": "20:00 - 20:30",
  "summary": "技术总监会议室猝死,凶手用录音伪造通话现场。",
  "incomingReport": [
    "20:30 保洁阿姨发现林泽远倒在小会议室,头部被钝器击伤。",
    "李婷(HR)20:15 经过会议室时听到说话声 — 死亡时间存疑。",
    "三个嫌疑人:陈昊(死者下属)/ 王凯(同组工程师)/ 李婷(HR)。"
  ],
  "suspects": [
    { "name": "陈昊", "role": "高级工程师,死者下属,会前最后谈话者" },
    { "name": "王凯", "role": "工程师,与死者有矛盾,偷录证据者" },
    { "name": "李婷", "role": "HR 专员,20:15 经过会议室的目击者" }
  ],
  "initialObjective": "先确认 20:15 会议室里到底发生了什么。",
  "riskLevel": "HIGH",
  "difficulty": "NORMAL",
  "investigationTags": ["企业命案", "时间诡计"],
  "briefingHeroImage": "<case-002 scenes 中合适的文件名>"
}
```

文本是占位,用户后续可微调。

### 6.3 investigationFlow

```json
"investigationFlow": {
  "initialNextAction": "进入会议室,了解现场情况。",
  "contradictionTriggers": [
    {
      "requiredClueIds": ["c001", "c009", "c010"],
      "thenSetFlag": "scene-investigated"
    },
    {
      "requiredClueIds": ["c002", "c006"],
      "thenSetFlag": "time-contradiction-found"
    }
  ],
  "flagBasedNextActions": [
    {
      "whenFlag": "time-contradiction-found",
      "thenShow": "20:15 不存在通话,但李婷听到了说话声 — 追问陈昊"
    },
    {
      "whenFlag": "scene-investigated",
      "thenShow": "现场证据已收集,继续调查办公区与走廊"
    }
  ],
  "suspectQuestionPrompts": [
    { "suspectId": "chenhao", "promptText": "陈昊:死者下属,可追问最后谈话内容" },
    { "suspectId": "wangkai", "promptText": "王凯:工程师,可追问当晚行踪" },
    { "suspectId": "liting", "promptText": "李婷:HR,可追问 20:15 听到的声音" }
  ]
}
```

注:`suspectQuestionPrompts` 的 suspectId 与 confrontation 的 suspect id
**不一定一致**。investigation 阶段李婷可问(她有 dialogue 字段),
confrontation 阶段李婷不进 tab(D2 决策)。两层用同一 suspectId 即可,
render 时按 phase 区分。

### 6.4 case-002 hotspot 数据补 onInteract.openOverlay

诊断报告 Bug 7 指出 chenhao-workstation hotspot 缺 openOverlay 效果。
Code 实施时检查 case-002 所有 hotspot 的 onInteract 字段:

- 缺 openOverlay 的:补上对应 overlay 配置(参考 case-001 hotspot 数据格式)
- 已有的:不动

具体 chenhao-workstation hotspot 应该触发什么 overlay?

候选:
- **A**:点击后弹出文本"陈昊工位简单整洁——相框里是一家三口的照片。仔细搜查抽屉?",
  并提供"搜查抽屉"按钮,点击后发现医院缴费单 clue
- **B**:点击后直接发现医院缴费单 clue,无中间步骤

我推荐 **A**(给玩家"搜查"的主动选择),symbol 与 testimony.md 第二部分
"陈昊工位 · 仔细搜查抽屉"描述对齐。

Code 实施时按 A 配置 hotspot.onInteract.openOverlay,具体 overlay 数据
结构参考 case-001 已有的 hotspot 配置。

## 7. 7 个 render 函数改造清单

每个函数的"原硬编码做什么"+ "改造后读什么字段"+ "case-001 / case-002 双 case 兼容"。

### 7.1 selector renderCard

**原**:`if (order === 1)` 渲染封面 div,case-002 永远没封面
**改**:`if (caseMeta.hero)` 渲染封面,从 hero.image 通过 getCaseAssetPath 拼路径
**双 case 兼容**:case-001 / case-002 数据都有 hero 字段后,两张卡都正确显示
**回归验证**:case-001 卡片视觉与现状一致(hero 图相同 + tagline "教学档案")

### 7.2 renderArchiveBody (archive 屏第二格)

**原**:第二格硬编码 "封存中" + "权限锁定" 占位 DOM
**改**:遍历 CASE_REGISTRY 渲染所有 case 的 archive 卡;当前 caseId 对应卡高亮,
       其他卡显示但灰色;若 case 未 unlocked 才显示"封存中"
**双 case 兼容**:case-001 / case-002 都 unlocked,两张卡都正确显示
**回归验证**:case-001 archive 屏现在有正确的"返回选择"按钮 + briefing 内容

### 7.3 renderArchiveBody (archive 屏第一格 briefing)

**原**:全文硬编码 case-001 文本
**改**:全部改读 caseConfig.briefing 字段(title / location / timeRange / summary /
       incomingReport / suspects / initialObjective / riskLevel / difficulty)
**双 case 兼容**:两 case 数据填了 briefing 字段后,各自渲染各自内容
**回归验证**:case-001 archive 屏文本逐字与现状一致

### 7.4 renderIntroBody

**原**:全文硬编码 case-001 briefing 屏(图 4 那种)
**改**:同 7.3,改读 caseConfig.briefing
**双 case 兼容**:两 case 都有 briefing 屏
**回归验证**:case-001 briefing 屏逐字不变

### 7.5 getNextActions

**原**:硬编码 "追问周岚" 等 case-001 文本
**改**:读 caseConfig.investigationFlow.flagBasedNextActions,
       根据当前激活的 flag 显示对应文本
**双 case 兼容**:flag 系统通用,两 case 各自定义自己的 flag 和文本
**回归验证**:case-001 收集 X clue 后显示"追问周岚..."与现状一致

### 7.6 evaluateFirstContradiction

**原**:硬编码 case-001 专属 clue ID 检查
**改**:遍历 caseConfig.investigationFlow.contradictionTriggers,
       检查 requiredClueIds 是否全部 collected,触发对应 flag
**双 case 兼容**:case-002 contradictionTriggers 数据驱动触发自己的 flag
**回归验证**:case-001 first-contradiction-found flag 触发时机不变

### 7.7 getHotspotState

**原**:硬编码 case-001 hotspot ID 映射
**改**:从 currentScene.hotspots 通用查找,根据 hotspot.id 和 onInteract 状态
       返回 idle / discovered / completed
**双 case 兼容**:两 case hotspot 都通过 case data 驱动
**回归验证**:case-001 hotspot 状态显示正确(已发现的高亮 / 未发现的暗显)

## 8. 实施清单

### (1) 改 src/stage1/types.ts
- CaseMeta 加 hero / tagline 可选字段
- StageCaseConfig 加 briefing / investigationFlow 可选字段
- 不动现有字段,SAVE_VERSION 不 bump

### (2) 改 src/cases/case-001/data.json
- 补 meta.hero / meta.tagline(reverse-engineer 现有硬编码)
- 补 briefing 字段(逐字反推)
- 补 investigationFlow 字段(反推 nextActions / 矛盾触发逻辑)
- **必须严格 1:1 对应现有硬编码文本,不允许"优化"或"重写"**

### (3) 改 src/cases/case-002/data.json
- 补 meta.hero / meta.tagline
- 补 briefing 字段(用占位文本,见 §6.2)
- 补 investigationFlow 字段(见 §6.3)
- 给 chenhao-workstation hotspot 补 onInteract.openOverlay(见 §6.4)
- 给所有缺 onInteract 的 hotspot 补上(参考 case-001 格式)

### (4) 改 src/stage1/app.ts(7 个 render 函数)
- selector renderCard
- renderArchiveBody (两段)
- renderIntroBody
- getNextActions
- evaluateFirstContradiction
- getHotspotState

每个函数改完后,必须保证 case-001 / case-002 都走数据驱动路径。
**不允许**"case-002 走数据驱动,case-001 保留硬编码"的混合模式。

### (5) 验证 Code 自查清单(必填,写在 commit message 里)

Code 必须自己 grep / view 验证以下事实:

```
☐ tsc --noEmit 全绿
☐ npm run build 成功
☐ Bundle 基线对比(以 35.16/6.61 为基线)
☐ JSON.parse 通过(case-001 + case-002 data.json 各自解析)
☐ grep 'case-001' src/stage1/app.ts → 应零命中(数据驱动后不应有 case-001 字符串)
☐ grep 'case-002' src/stage1/app.ts → 应零命中
☐ grep '周岚' src/stage1/app.ts → 应零命中(已搬到 data.json)
☐ grep '陈昊' src/stage1/app.ts → 应零命中
☐ grep '会议室' src/stage1/app.ts → 应零命中(场景名搬到 data.json)
☐ 启动期 dev server console:无 case-001 / case-002 校验报错
☐ 7 个 render 函数现场代码,各贴 5-10 行片段证明读 caseConfig 而非硬编码
☐ case-001 data.json briefing.summary 字段值与原硬编码逐字相等
☐ case-001 data.json briefing.incomingReport 数组各元素与原硬编码逐字相等
☐ case-001 data.json briefing.suspects 数组各元素与原硬编码逐字相等
☐ case-001 data.json briefing.initialObjective 与原硬编码逐字相等
☐ case-001 data.json investigationFlow.contradictionTriggers 与原 evaluateFirstContradiction 行为等价
☐ case-001 data.json investigationFlow.flagBasedNextActions 与原 getNextActions 输出等价
```

每条都要在 commit message 中明确打勾或标注异常。

## 9. Bundle 预算

T2.8 后基线:JS gzip 35.16 / CSS gzip 6.61

T2.8.1 预算:
- 7 个 render 函数改造:JS +1-2 kB(数据驱动逻辑略复杂)
- types.ts 新字段:可忽略
- case-001 data.json 补字段:JSON 增长约 2-3 kB,gzip 后 +0.5-1 kB
- case-002 data.json 补字段:同上 +0.5-1 kB
- **总预算**:JS gzip +2-4 kB

合理上限:JS gzip ≤ 39 kB(基线 35.16 + 4 kB)

## 10. 风险点

### 高风险
- case-001 数据反推填错,导致 case-001 行为变化 → 用户验收时 case-001 走 happy path,
  逐字核对 result 屏文本(标题"案件归档" + 周岚 body)
- 缓解:Code 自查清单第 12-17 项强制逐字校对

### 中风险
- 7 个 render 函数改造范围大,可能漏改某个分支 → grep 'case-001' / '周岚' / '陈昊' /
  '会议室' 验证零命中作为兜底
- 缓解:第 8 节实施清单 (5) 项 grep 自查

### 低风险
- case-002 文案是占位,用户验收时可能觉得不够好 → 接受,用户后续微调

## 11. 提交节奏

1. chore(t2.8.1): pre-flight checks(空 commit)

2. feat(t2.8.1): refactor render layer to data-driven for multi-case support
   - 包含 (1)-(4) 项改动
   - commit message 必含:
     * 文件改动清单 + 行数 stat
     * 17 项 Code 自查清单逐项打勾
     * Bundle 基线对比表
     * tsc / build / JSON.parse 通过状态
     * 8 个 bug 各自的"代码修复说明"(改了哪些行 + 修了哪个 bug)
     * 诚实声明:未在浏览器实测,真实跑通靠用户验收

3. docs(t2.8.1): document data-driven render layer migration
   - 新建 docs/t2.8.1-render-data-driven-notes.md
   - 内容:T2.8.1 范围 / 8 个 bug 修复对应表 / types.ts 扩展记录 /
     case-001 反推填字段方案 / case-002 占位字段说明 /
     KNOWN_ISSUES 新增项

## 12. Code 实施中必须停下来汇报

按 T2.8-task.md 第 13 节 + 以下额外:

- case-001 现有硬编码文本无法精确 grep 定位 → 停下汇报哪几处
- case-002 缺某字段需要的素材文件不存在 → 停下汇报
- types.ts 扩展引发现有 case-001 校验失败 → 停下汇报
- grep 'case-001' / '周岚' 等 hard-string 在 src/stage1/app.ts 中
  无法清零(可能有合理硬编码场景)→ 停下汇报具体位置 + 是否合理

任何情况下,Code 不得自创 briefing 文本(必须从硬编码反推 case-001 /
从剧本第一段抄录 case-002)。

## 13. 验收强度纪律

### Code 责任(代码层)
- 上述 17 项自查清单全部打勾
- commit message 诚实声明"未在浏览器实测"
- 8 个 bug 修复点逐项贴关键代码片段

### 用户责任(浏览器实测)
T2.8.1 合 main 前必须真人浏览器走过:

**case-001 回归路径**:
- selector 卡片视觉(hero 图 + tagline 与现状一致)
- 进入 case-001 archive(briefing 文本逐字与现状一致)
- 进入 investigation(briefing 屏内容一致)
- 收集 clue → next action 提示文本与现状一致
- 走完 happy path → result 屏"案件归档" + 周岚 body 逐字一致

**case-002 修复路径**:
- selector 看到 case-002 卡片有专属 hero 图(不是 case-001 那张)
- 点 case-002 卡 → archive 屏右侧不再"封存中",而是 case-002 briefing 内容
- briefing 文本是 case-002 的(陈昊 / 王凯 / 李婷,不是周岚 / 陈序)
- 进入 investigation → 看到 briefing 屏(case-002 自己的内容)
- 收集 clue → 下一步引导文本是 case-002 的(不是"追问周岚")
- "可追问人物"显示陈昊 / 王凯 / 李婷
- 点击陈昊工位 hotspot → 弹出 overlay,可"搜查抽屉"发现医院缴费单
- 走到 confrontation → 王凯 tab + 陈昊 tab 锁定,符合 T2.8 unlocksSuspect 设计

任一不达标 → 反馈给我做归因 + Code hotfix。

### Push 红线(再重申)
不 push。push 操作必须由用户在对话中输入"push"作为单独指令后才能执行。
"等用户审阅"不等于"等用户审完自己 push","审阅"和"push"是两个独立的用户动作。
历史违反:974a015 / 8ccffd1 两次 PROJECT_STATE 自主 push。第三次违反将记入永久案例。

---

(T2.8.1-task.md 完)